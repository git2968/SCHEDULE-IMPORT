{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/backImg/(.*)", "dest": "/backImg/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite"
} 